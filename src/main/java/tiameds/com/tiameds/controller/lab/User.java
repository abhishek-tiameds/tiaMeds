package tiameds.com.tiameds.controller.lab;


import io.swagger.v3.oas.annotations.tags.Tag;
import org.springframework.web.bind.annotation.RestController;

@RestController("/admin/lab/user")
@Tag(name = "User Controller", description = "This controller is responsible for user management in the Lab Management System")
public class User {

      //=================== list of endpoints to be implemented ===================
        // 1.create user for lab
        // 2.get all users in the lab
        // 3.get user by id
        // 4.update user
        // 5.delete user
        // 6.assign role to user
        // 7.assign module to user
        // 8. delete role from user for lab
        // 9. delete module from user for lab
        // 10. get all roles for user






}
